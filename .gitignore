# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Cache
cache/
# JSON artifacts (NOT package manifests or configs)
coverage/
*.coverage.json
server/src/data/cache/*.json
server/src/quant/cache/*.json
server/src/data/stockMaster.json
# 对话长期记忆（运行时生成，不入库）
server/src/data/chatHistory.json
server/data/chatHistory.json
# 合规审计日志（运行时生成，不入库）
server/src/data/audit.log
# 研究历史记录（运行时生成，不入库）
server/src/data/history.json
# 分析断点续跑中间产物（运行时生成，不入库）
server/src/data/checkpoints/
# 评级事后校准台账（运行时生成，不入库）
server/src/data/outcomes.json
.coverage_tmp/

# Test artifacts
test-screenshots/
test-api-response.txt

# Environment
.env
.env.local

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# Playwright
.pw-browsers/
test-results/
playwright-report/
e2e/.tmp/

# 运行时数据（watchlist 等）
server/data/
server/src/data/watchlist.json

# UI screenshot artifacts (dev only)
_shots/

# 临时截图/脚本（README 截图生成中间产物）
_shots2/

# WorkBuddy 运行时目录（工具数据/记忆，非项目源码，避免误提交）
.workbuddy/
