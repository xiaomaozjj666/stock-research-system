# 依赖构建脚本白名单：只放行 esbuild（vite/tsx 依赖它的 postinstall 落二进制）。
# 说明：树中只有 esbuild@0.28.1（此前还列了 0.21.5 / 0.25.12 两个已不在树里的版本，
# 属死条目）。CI 用 `--dangerously-allow-all-scripts` 会整体绕过本白名单，
# 这里是本地 `npm install`（不带该参数）时的最小放行集。
allow-scripts=esbuild@0.28.1
